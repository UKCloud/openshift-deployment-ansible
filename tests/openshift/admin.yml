- hosts: localhost
  tasks:

  - name: Login to openshift
    command: oc login https://ocp.{{domainSuffix}}:8443 -u "{{ADMIN_USERNAME}}" -p "{{ADMIN_PASSWORD}}" --insecure-skip-tls-verify

  - name: test top nodes
    command: oc adm top nodes

  - name: test top pods
    command: oc adm top pods

  - name: register node
    shell: oc get node | grep -v NAME | awk 'NR==1{print $1}'
    register: nodeName

  - name: test manage-node
    command: oc adm manage-node {{ nodeName }} --schedulable 
