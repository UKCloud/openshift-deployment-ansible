---
- hosts: localhost
  vars:
#     b1: "{{ ansible_env.bastionip }}"
     b2: "{{ lookup('env','bastionip') }}"
     b3: "{{ ansible_env.bastianHost }}"
  tasks:

#  - debug: msg={{ b1 }}
  - debug: msg={{ b2 }}
  - debug: msg={{ b3 }}
  - set_fact: hostIP={{ lookup('env','bastionip') }}
  - debug: msg={{ hostIP }}
  - name: Test monitoring web page is available
    uri: 
      url: http://{{ hostIP }}
      status_code: 200
